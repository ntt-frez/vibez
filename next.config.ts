import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Hides the default bottom-left “N” dev badge (full-screen map UI lives there).
  devIndicators: false,
};

export default nextConfig;
